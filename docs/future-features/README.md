# Future Features — full specs

One file per feature: `FF-NNN-slug.md`, created from `_TEMPLATE.md`. Lightweight stubs live in
the registry at `../FUTURE_FEATURES.md`. **Don't start building until a spec's Status is
`Ready for Build`.** Launch with `/ff <name>`. Standard: `~/.claude/DOCUMENTATION_STANDARDS.md`.
